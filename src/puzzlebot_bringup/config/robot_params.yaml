/**:
  ros__parameters:
    wheel_radius: 0.05          # metres
    wheel_separation: 0.19      # metres — distance between wheel contact points
    input_source: encoders      # encoders | joint_states
    odom_topic: odom_raw
    odom_frame: odom
    base_frame: base_footprint
    publish_tf: false           # kalman_filter_node owns odom -> base_footprint
