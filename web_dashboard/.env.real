# Preset: Physical robot
# Replace BRIDGE_IP with the IP of the computer running puzzlebot_web_bridge.
# Find it with: ip addr show | grep "inet " | grep -v 127
VITE_WS_URL=ws://BRIDGE_IP:8000/ws
VITE_ROBOT_ENV=real
